meta {
  name: "Upsert User"
  type: "http"
  seq: 2
}

request {
  method: "POST"
  url: "{{apiBase}}/users"

  headers {
    Accept: "application/json"
    Content-Type: "application/json"
    X-Internal-Api-Key: "{{internalApiKey}}"
  }

  body: json({
    "id": {{defaultUserId}}
  })
}
