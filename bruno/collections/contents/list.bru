meta {
  name: "List Contents"
  type: "http"
  seq: 3
}

request {
  method: "GET"
  url: "{{apiBase}}/contents?user_id={{defaultUserId}}&page=1&size=20"

  headers {
    Accept: "application/json"
    X-Internal-Api-Key: "{{internalApiKey}}"
  }
}
